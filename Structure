#define PASS_PROF "cytech2026"

typedef struct {
    char nom[50];           /* Nom du QCM (ex: "Maths") */
    int points_negatifs;    /* 1 = Oui pour tout le QCM, 0 = Non */
    int reponses_multiples; /* 1 = Oui (ex: "ab"), 0 = Non (ex: "a") */
    int mode_sequentiel;    /* 1 = Reponse obligatoire, 0 = On peut passer */
    int nb_questions;       /* Nombre total de questions dans ce fichier */
} Parametres;

typedef struct {
    char intitule[500];     /* L'enonce */
    char props[4][200];     /* Les 4 choix (a, b, c, d) et jusuqu'a 200 caractères par question */
    char bonnes_reponses[5]; /* La chaine de caracteres de la solution */
} Question;


J'ai pas mis les reponses multiples à voir. IL y a marqué que si ya des points négatifs c'est pour tout le QCM. 




(((((( int reponses_multiples; /* 1 = Oui (ex: "ab"), 0 = Non (ex: "a") */

char bonnes_reponses[5]; /* La chaine de caracteres de la solution */       ))))))))))
